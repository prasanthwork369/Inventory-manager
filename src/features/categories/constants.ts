/**
 * Copied verbatim from the web reference's Categories.tsx `icons` array —
 * same 12 emoji, same order, same default for a new category.
 */
export const CATEGORY_ICON_OPTIONS = ['🥤', '🔌', '🛒', '👕', '🧴', '🔧', '📦', '🍫', '🧻', '💊', '🎒', '🪑'] as const;

export const DEFAULT_CATEGORY_ICON = '📦';
